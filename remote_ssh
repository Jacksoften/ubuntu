# scp could be used for downloading and uploading files through ssh
scp <filename> <servername>

```{bash}
# download: remote -> local
scp user@remote_host:remote_file local_file

# upload: local -> remote
scp local_file user@remote_host:remote_file
```
